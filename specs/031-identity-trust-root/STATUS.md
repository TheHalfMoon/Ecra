# ECR-031 Status — Identity, Trust Root & Sensitive Storage Foundations

**Slice:** ECR-031  
**Lifecycle:** IMPLEMENTING  
**Dependencies:** ECR-001 `CLOSED_CANONICAL`, ECR-002 `CLOSED_CANONICAL`  
**Canonical implementation base:** `f6d8eb6ff6a60aa0ad8a6f52686a62f12cd374b0`  
**Implementation branch:** `031-identity-trust-root`  
**Draft implementation PR:** #4  
**Exact-head ECR-001 planning gate:** `33158268342` — SUCCESS  
**Exact-head ECR-002 planning gate:** `33158268371` — SUCCESS  
**Analyze Pass 1:** `44e85aa9ccd28e185a5761889aa12b50459f286e` — `PLANNING_REWORK_REQUIRED`  
**Analyze Pass 2:** `a3c7d563c139c65886f169f9181c07a997038f1f` — `ZERO_BLOCKING_PLANNING_DRIFT_FOUND`  
**Requirements checklist:** PASS_FOR_ANALYZE_PASS_2  
**Constitution:** v1.1.0

The final synchronized planning head `f6d8eb6ff6a60aa0ad8a6f52686a62f12cd374b0` passed both permanent closed-slice regression workflows and is the exact implementation base. ECR-031 is authorized to implement on `031-identity-trust-root` in `tasks.md` dependency order.

## Roadmap-owned outcome

```text
IdentityAssertion validation
Actor ↔ Principal / on-behalf-of binding
Device/user-local trust root
Key issue/rotation/revocation
Protected authenticity/signature envelope primitives
Sensitive local-storage protection contracts
```

## Pass-1 remediation closure

```text
C1 local bootstrap / enrollment                 REMEDIATED
C2 protected authoritative trust snapshot       REMEDIATED
C3 non-ambient assertion issuance               REMEDIATED
C4 portable Ed25519 signing custody              REMEDIATED
C5 tasks/fixtures/test ownership                 REMEDIATED
C6 checklist + Analyze Pass 2                    REMEDIATED

UNOWNED_FR=0
UNOWNED_SC=0
MUST_LEVEL_PLANNING_GAPS=0
FAILED_CONSTITUTION_GATES=0
IMPLICIT_CRITICAL_RISK_ACCEPTANCE=0
```

## Frozen v1 decisions

### Identity / bootstrap

- Reuse ECR-001 `PrincipalRef`, `PrincipalId`, `IdentityAssertionRef`, `IdentityAssertionId`, `Actor`, and `ActorId`.
- Bootstrap creates an opaque **Ecra-local installation principal**. It does not claim external/legal/NIST identity proofing.
- `PrincipalId` is generated and never derived from OS username/email/display labels.
- Bootstrap is complete only after protected backend material + authenticated protected trust-state durable publish + authenticated reopen.
- Partial bootstrap yields typed `incomplete_bootstrap`; no silent second principal/root mint.

### Lifecycle authority

- `ProtectedTrustStateV1` is authoritative for enrollment/current key generation/retirement/revocation.
- Ordinary DB/file metadata is non-authoritative/rebuildable.
- Only authenticated protected state can produce `VerifiedTrustSnapshot` for validation/issuance.
- V1 does not claim universal monotonic rollback resistance against restoration of an older valid protected state plus equivalent authorized keystore state.

### Issuance

- No generic `issue(arbitrary_principal_id, ...)` API.
- `EnrolledPrincipalHandle` + current `VerifiedTrustSnapshot` create a non-serializable, process-local `IssuerSession` fixed to one principal/root/key.
- Caller may request actor/audience binding but cannot substitute the subject principal.
- V1 cannot mint arbitrary on-behalf-of principals.
- No ECR-031 IPC/network assertion-minting service exists.

### Cryptographic custody

- Canonical v1 assertion/protected-anchor signing algorithm: Ed25519.
- The software signing key is generated by production CSPRNG and stored only as native-backend-protected secret material.
- It may be materialized only for bounded signing inside redacted/zeroizing secret handling.
- Portable v1 does **not** claim Secure Enclave/hardware-backed/non-exportable signing.
- Protected envelopes use the frozen ChaCha20-Poly1305/HKDF-SHA-256 direction.

## Hard boundaries

ECR-031 MUST NOT implement or counterfeit:
- general authorization, capability narrowing, declassification, approval or execution leases — ECR-003;
- independent verification/reconciliation decisions — ECR-004;
- browser/model/tool/process/provider execution;
- protocol token passthrough or MCP/ACP/A2A gateway semantics — ECR-016;
- local-model gateway behavior — ECR-021;
- multi-device encrypted sync/recovery — ECR-022;
- telemetry/privacy product controls — ECR-025;
- general import/export portability — ECR-029.

A validated identity assertion answers **who / on whose behalf / under which trust root and bounded assertion context**. It never means **what is authorized**.

## Planning package

```text
spec.md                         FR-001–FR-058 / SC-001–SC-016
research.md                     R1–R17 + T001 implementation-time dependency review
data-model.md                   enrollment/issuer/trust snapshot/key/envelope/anchor model
contracts/identity-trust-v1.md normative v1 wire and fail-closed rules
threat-model.md                 T1–T23 including bootstrap/rollback/ambient-mint threats
plan.md                         one bounded ecra-identity crate + G1–G15
quickstart.md                   exact implementation verification contract
tasks.md                        T001–T082 executable dependency order
checklists/requirements.md      PASS_FOR_ANALYZE_PASS_2
analyze.md                      Pass 2 ZERO_BLOCKING_PLANNING_DRIFT_FOUND
```

## Phase 1 materialized state

The Phase 1 implementation surface is now present, but it is **not yet declared T009 PASS** until a permanent ECR-031 workflow succeeds on the exact current head.

```text
T001  dependency/version/license/advisory/MSRV review recorded
T002  one crates/ecra-identity trusted crate; ecra-core-only initial boundary preserved in its atomic creation commit
T003  reviewed minimal crypto/macOS dependency manifest adopted; exact Cargo.lock generated on trusted runner and committed by github-actions[bot]
T004  scripts/check-identity-unsafe.sh materialized
T005  scripts/check-identity-deps.sh materialized
T006  .github/workflows/ecr-031.yml materialized as trusted push-only branch/main workflow
T007  explicit ECR-031 phase-owned target hooks materialized; targets become mandatory when their owning task creates the test file
T008  Rust/Cargo, lock digest, manifest feature assertions and cargo-tree evidence materialized
T009  PENDING exact-head permanent ECR-031 CI
T010  PENDING exact-head dependency/license/native-boundary disposition after T009
```

Lock generation evidence:

- helper workflow run `33161331512` at `88c6c04d97c35cef50fd9ad4f16bc44246cf5175` failed before jobs because its YAML condition was invalid;
- repair commit `750725febf45c364e8973b82d0b31ffd0ed059ed` removed the invalid condition without rewriting history;
- trusted helper workflow run `33161386983` completed SUCCESS and generated/checked the exact dependency graph;
- `github-actions[bot]` committed `Cargo.lock` and removed the temporary helper in `48253d3e28caf94b00c6699609b7befa4f717804` with message `build(identity): lock reviewed dependency graph`;
- permanent Phase 1 boundary commit `5ab816102c16242df7cd1de2297c3b5217fc5396` added T004–T008 surfaces on top of that lock commit.

The temporary helper workflow is not part of the permanent trust surface.

## Current execution frontier

1. require the permanent `.github/workflows/ecr-031.yml` to run and succeed on the exact current branch head with build/fmt/strict Clippy/workspace tests/rustdoc/offline, ECR-001/ECR-002 regressions, ECR-031 boundaries and dependency/toolchain evidence;
2. if the run fails, repair the exact failing step without weakening a gate;
3. after exact-head success, complete T009 and record T010 locked dependency/license/native-boundary evidence in this status and `research/donor-license-ledger.md`;
4. synchronize `tasks.md` Phase 1 checkboxes only to evidence actually satisfied;
5. immediately begin T011 and continue Phase 2 in dependency order.

No task is marked complete because code merely exists. `VERIFIED_ON_BRANCH` will not be treated as `CLOSED_CANONICAL`.